
import 'package:flutter/material.dart';

class ARSection extends StatelessWidget {
  const ARSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
